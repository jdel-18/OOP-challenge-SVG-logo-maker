function renderLogoShape(shape) {
    if(shape === '') {
        return '';
    }
}
function renderLogoShape(shape) {
    if(shape === '') {
        return '';
    }
}
function renderLogoShape(shape) {
    if(shape === '') {
        return '';
    }
}